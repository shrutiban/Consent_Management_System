package com.iiitb.ConsentManagement.ConsentManagement.Beans;

import org.springframework.stereotype.Component;

import javax.persistence.*;

//@Entity
@Component
//@Table(name="activity_rules_table")
public class ActivityRules {

 /*   enum OPERATION {
        CREATE,
        READ,
        UPDATE,
        DELETE
    }

    enum LOCATION
    {
        HOSPITAL
    }



    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Integer ID;     // This is auto generated ID

    @Column(nullable = false,unique = true)
    private String activityRuleID;

    // This will point to the roles in the roles table.
    //@ManyToOne(cascade = CascadeType.ALL)
   // @JoinColumn(name="roleID", referencedColumnName = "roleID")
    private Actor roleID;

    @Column(nullable = false)
    @Enumerated(EnumType.STRING)
    private OPERATION operation;

    @Column(nullable = false)
    @Enumerated(EnumType.STRING)
    private LOCATION location;      // as of now only hospital

    // here there is one more field called record which is the one we will be accessing


*/

}
